"""
Dataset generation is intentionally disabled.

The dashboard reads only data/sales_data.csv at runtime. To use audited real-world
business data, replace that CSV with an export that follows the documented schema.
"""

raise SystemExit(
    "Random data generation is disabled. Update data/sales_data.csv with your verified sales export instead."
)