"""
Dataset generation is intentionally disabled.

The dashboard reads only the active uploaded file or data/sales_data.csv at runtime.
To use audited real-world business data, upload/replace the CSV or Excel source.
"""

raise SystemExit(
    "Random data generation is disabled. Upload or replace the sales data source instead."
)